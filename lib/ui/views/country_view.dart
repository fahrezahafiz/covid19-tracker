import 'package:flutter/material.dart';

class CountryView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Country View\nCountry: '),
      ),
    );
  }
}
